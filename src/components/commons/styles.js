import styled from 'styled-components'

export const StHeading2 = styled.h2`
  font-size: 3.2rem;
  font-weight: 400;
  line-height: 42px;
  letter-spacing: 0em;
  text-align: left;
  color: ${({ theme }) => theme.COLORS.WHITE};
`
